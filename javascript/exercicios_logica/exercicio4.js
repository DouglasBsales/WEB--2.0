/*Esse algoritmo está recebendo o número inteiro e passando o seu 
anterior e o seu posterior
*/

let numeroInt = 50;

let numeroAnterior = numeroInt - 1;
let numeroPosterior = numeroInt + 1;

console.log(
  `Numero anterior: ${numeroAnterior}. Numero inteiro: ${numeroInt}. Numero posterior: ${numeroPosterior}`
);
